<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cth.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".presentation.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.CourseActivity"/>
        <activity android:name=".presentation.CourseSelectionActivity"
            android:noHistory="true"/>
        <activity android:name=".presentation.PlanListActivity"
            android:noHistory="true"/>
        <activity android:name=".presentation.PlanOptionsActivity"/>
        <activity android:name=".presentation.SemesterSelectionActivity"
            android:noHistory="true"/>
        <activity android:name=".presentation.CurrentSemesterActivity"/>
        <activity android:name=".presentation.CompletedSemestersActivity"/>
        <activity android:name=".presentation.UpcomingSemestersActivity"/>
    </application>
</manifest>